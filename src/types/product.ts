export type ProductCommand = {
  id: string;
  name: string;
  price: number;
  category: string;
};

export type ProductViewModel = {
  id: string;
  name: string;
  price: number;
  category: string;
};
